@model IEnumerable<Asp5.Models.Category>
@using System.Linq

@{
    ViewBag.Title = "Страница категорий";
}

<h1>Список категорий</h1>

<a asp-action="CreateCategory" asp-controller="Home" class="btn btn-success">Добавить категорию</a>

<table class="table table-striped mt-3 categories-table">
    <thead>
        <tr>
            <th>Название</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @if (!Model.Any())
        {
            <tr>
                <td colspan="2" class="text-center">Категории не найдены.</td>
            </tr>
        }
        else
        {
            foreach (var category in Model)
            {
                <tr>
                    <td>@category.CategoryName</td> 
                    <td>
                        <a asp-action="DetailsCategory" asp-route-id="@category.CategoryId">Детали</a> |
                        <a asp-action="EditCategory" asp-route-id="@category.CategoryId">Изменить</a> |
                        <a asp-action="DeleteCategory" asp-route-id="@category.CategoryId" onclick="return confirm('Вы уверены, что хотите удалить эту категорию?');">Удалить</a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>